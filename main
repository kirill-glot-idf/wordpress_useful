<? echo the_permalink( ); ?>
<? echo wp_get_attachment_image( 77 , 'full', "", array( "class" => "" )); ?>

<? $casino_main_logo = get_field('casino_main_logo'); ?>
<?php echo $casino_main_logo['url']; ?>
<?php echo $casino_main_logo['alt']; ?>


Обычные поля
<? the_field('external_link'); ?>

Группа
//ИЗО	БРАЖЕНИЕ
<a href="<? echo $field['four_screen_4']['url']?>" data-fancybox>
  <img src="<? echo $field['four_screen_4']['url']?>" alt="<? echo $field['four_screen_4']['url']?>" />
</a>


<?php
	$field=get_field('q_and_a');
		if( $field ):  ?>
		
		<div class="answer_wrapper">
          <div class="title">
            <div class="number">1</div>
            <h3><? echo $field['title_1']?></h3>
          </div>
          <div class="content">
            <? echo $field['answer_1']?>
          </div>
        </div>

		  <?
	endif;
?>










		while ( have_rows('right_carousel') ) : the_row();?>
			<div class="review_carousel_item">
				<? $mini_logo = get_sub_field('mini_logo'); ?>
				<img src="<?php echo $mini_logo['url']; ?>" alt="<?php echo $mini_logo['alt']; ?>" class="logo_review_carousel">
				<? $main_image = get_sub_field('main_image'); ?>
				<img src="<?php echo $main_image['url']; ?>" alt="<?php echo $main_image['alt']; ?>" class="review_carousel_main_img">
			</div>
	   <? endwhile;




<? the_field('are_they_polite'); ?>
<? the_field('do_they_give_bonuses_in_chat'); ?>
<? the_field('are_they_fast'); ?>
<? the_field('are_they_good_in_their_job'); ?>
<? the_field('is_their_english_adequate'); ?>
<? the_field('does_the_chat_start_quickly'); ?>
Повторитель


<?php
if( have_rows('right_carousel') ):
		while ( have_rows('right_carousel') ) : the_row();?>
			<div class="review_carousel_item">
				<? $mini_logo = get_sub_field('mini_logo'); ?>
				<img src="<?php echo $mini_logo['url']; ?>" alt="<?php echo $mini_logo['alt']; ?>" class="logo_review_carousel">
				<? $main_image = get_sub_field('main_image'); ?>
				<img src="<?php echo $main_image['url']; ?>" alt="<?php echo $main_image['alt']; ?>" class="review_carousel_main_img">
			</div>
	   <? endwhile;
	else :
endif;
?>


Гибкое содержание
<?php	
	if( have_rows('main_page_galary') ):
		while ( have_rows(menu_number_list) ) : the_row();
			if( get_row_layout() == 'item_list' ):
			  ?> 
				<div class="item_pretitle"><? the_sub_field('pretitle'); ?></div>
			  <?
			endif;
		endwhile;
	else :
	endif;
?>	
	

//do shortcode
<?php echo do_shortcode('[elementor-template id="416"]'); ?>
//search form	
<?php get_search_form(); ?>


//wpdb out file
<?php

global $wpdb;

if(!isset($wpdb))
{
    require_once('../../../wp-config.php');
    require_once('../../../wp-includes/wp-db.php');
}

$wpdb->insert(
    'wp_postmeta', // указываем таблицу
    array(
        'post_id' => 666,
        'meta_key' => 'text_ajax 1',
        'meta_value' => 'ajax value 4'
    ),
    array('%d', '%s', '%s')
);

////Вывод по категориям
<?
$query = new WP_Query( array( 'category_name' => 'best_no_deposit' ) );
$counter = 0;
while ( $query->have_posts() and $counter<2 ) {
  $query->the_post();
  ?>
<div class="bonus_vertical_item">
	<? echo wp_get_attachment_image( 252 , 'full', "", array( "class" => "bonus_vertical_item_bg" )); ?>
  <div class="logo_wrapper black_c" style="background: <?echo get_field('logo_background_color')?>">
	  <? $image = get_field('review_games_casino_logo') ?>
	<img src="<?echo $image['url']?>" alt="<?echo $image['alt']?>">
  </div>

  <div class="free_spins_counter_wrapper">
	<div class="counter">
		<? echo get_field('approv_free_spins')?>
	</div>
	<div class="title">
	  FREE<br>
	  SPINS
	</div>
  </div>
  <div class="counter_desc">
	NO DEPOSIT BONUS
  </div>
  <a href="<? the_field('external_link'); ?>" target="_blank" class="button_green_md">
		  <span class="for_hover"></span>
	<? echo wp_get_attachment_image( 255 , 'full', "", array( "class" => "button_green_md_img" )); ?>
	<span class="button_yellow_md_span">CLAIM</span>
  </a>
</div><!-- end bonus_vertical_item -->

  <?

  $counter++;
}
wp_reset_postdata();
?>

// in functions.php
remove_action( 'wp_head', 'feed_links_extra', 3 ); // Display the links to the extra feeds such as category feeds
remove_action( 'wp_head', 'feed_links', 2 ); // Display the links to the general feeds: Post and Comment Feed
remove_action( 'wp_head', 'rsd_link' ); // Display the link to the Really Simple Discovery service endpoint, EditURI link
remove_action( 'wp_head', 'wlwmanifest_link' ); // Display the link to the Windows Live Writer manifest file.
remove_action( 'wp_head', 'index_rel_link' ); // index link
remove_action( 'wp_head', 'parent_post_rel_link', 10, 0 ); // prev link
remove_action( 'wp_head', 'start_post_rel_link', 10, 0 ); // start link
remove_action( 'wp_head', 'adjacent_posts_rel_link', 10, 0 ); // Display relational links for the posts adjacent to the current post.
remove_action( 'wp_head', 'wp_generator' ); // Display the XHTML generator that is generated on the wp_head hook, WP version
remove_action('wp_head', 'print_emoji_detection_script', 7);
remove_action('wp_print_styles', 'print_emoji_styles');
remove_action( 'admin_print_scripts', 'print_emoji_detection_script' );
remove_action( 'admin_print_styles', 'print_emoji_styles' );

git init
git add .
git commit -m "first commit"
git remote add origin https://github.com/KirylHlot/wordpress_starter_pack.git
git push --set-upstream origin master


<a href="" data-fancybox="images">
	
</a>



		  echo '<pre>';
		  print_r($reclam);
		  echo '</pre>';
		  $reclam -> query_vars['offset'] = $offset_counter;
		  echo $reclam -> query_vars['offset'];


	    <?
	    $query = new WP_Query( array( 'category_name' => 'review_top' ) );
	    $counter = 0;
	    $page_id = get_the_ID();
	    while ( $query->have_posts() and $counter<2 ) {
	    $query->the_post();
	    $this_id = get_the_ID();
	    if($page_id != $this_id){
	    ?>
        <div class="review_bonus_item" style="background: <? the_field('logo_background_color'); ?>">
	        <? $casino_logo = get_field('casino_main_logo'); ?>
          <img src="<?php echo $casino_logo['url']; ?>" alt="<?php echo $casino_logo['alt']; ?>" class="logo">
          <div class="content">
	          <? the_field('bonus_text_2'); ?>
          </div>
          <a href="<? the_field('external_link'); ?>" target="_blank" class="button">
            Get your bonus NOW
          </a>
        </div>

      <?$counter++;
        }
      }
      wp_reset_postdata();
      ?>
	  
	  
	  
	  
	  
	  



<?
$query = new WP_Query( array( 'category_name' => 'review_left_bottom_banners' ) );
$counter = 0;
while ( $query->have_posts() and $counter<10 ) {
$query->the_post();
?>

<div class="review_bonus_item" style="background: <? the_field('logo_background_color'); ?>">
      <? $casino_logo = get_field('casino_main_logo'); ?>
<img src="<?php echo $casino_logo['url']; ?>" alt="<?php echo $casino_logo['alt']; ?>" class="logo">
	<? the_field('bonus_text_2'); ?>
</div>
<a href="<? the_field('external_link'); ?>" target="_blank" class="button">
  Get your bonus NOW
</a>
</div>

<?$counter++;
}
wp_reset_postdata();
?>
      
      
//пагинация
//WP-PageNavi
<?php
  $current_page = (get_query_var('paged')) ? get_query_var('paged') : 1;
  $params = array(
    'posts_per_page' => 1, // количество постов на странице
    'category_name'       => 'video', // тип постов
    'paged'           => $current_page // текущая страница
  );
  query_posts($params);

  $wp_query->is_archive = true;
  $wp_query->is_home = false;

  while(have_posts()): the_post();
  ?>
	//код здесь
  <?
  endwhile;?>
  //пагинация здесь
<?wp_pagenavi();?>

// Обрезка контента с html тегами
$more = ' <a href="'. get_permalink( get_the_ID() ) . '" style="font-weight: bold; color: #ff5253;">Читать полностью</a>';
echo force_balance_tags( html_entity_decode( wp_trim_words( htmlentities( get_the_content() ), 200, $more ) ) );
